# Experiment Configs

Experiment configs will live here.

Initial targets:

- `smoke_prior.yaml`;
- `prior_scorecards.yaml`;
- `multiview_pairs.yaml`;
- `encoder_pretraining.yaml`;
- `encoder_tabpfn.yaml`;
- `icl_task_sampler.yaml`.

