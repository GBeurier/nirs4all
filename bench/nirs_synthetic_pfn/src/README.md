# Source

Self-contained bench package code will live here.

The planned package name is `nirsyntheticpfn`.

Production `nirs4all` code should be imported as a dependency and reference. Do
not copy production modules into this directory as a fork.

