# Data Contracts

Planned modules:

- `tasks.py`: `NIRSPriorTask`;
- `latents.py`: `CanonicalLatentBatch`;
- `views.py`: `SpectralViewBatch`;
- `metadata.py`: provenance and validation metadata helpers.

Contracts should stay explicit and testable. Early approximations are allowed,
but hidden state is not.

