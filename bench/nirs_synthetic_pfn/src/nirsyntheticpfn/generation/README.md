# Generation

Planned modules:

- `dataset_factory.py`: build `SyntheticDatasetRun` objects;
- `multiview_factory.py`: render several views from common latents;
- `shard_writer.py`: write pretraining shards with provenance.

Use `nirs4all.synthesis` as the engine when possible.

