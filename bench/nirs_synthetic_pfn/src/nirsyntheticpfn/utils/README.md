# Utilities

Planned modules:

- `config.py`: load and validate experiment configs;
- `seeds.py`: deterministic seed handling;
- `provenance.py`: git, command, and environment summaries.

Keep utility behavior deterministic and easy to test.

