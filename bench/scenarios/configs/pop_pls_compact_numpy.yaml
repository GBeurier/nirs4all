# bench/scenarios/configs/pop_pls_compact_numpy.yaml
#
# Owner: Agent C / MLOps Spine. Bound to registry entry `POP-PLS-compact-numpy`.
# Status: SKELETON, sub-decision under D-C-010. DECISION_PENDING_CODEX_REVIEW.

canonical_name: POP-PLS-compact-numpy
schema_version: 0.1.0
codex_review_status: DECISION_PENDING_CODEX_REVIEW

model:
  pipeline:
    - model:
        class: nirs4all.operators.models.sklearn.pop_pls.POPPLSRegressor
        params:
          random_state: 0

hyperparameter_search:
  protocol: model_native
  n_splits: null
  param_grid: null

dispatch:
  n_jobs: 1
  verbose: 0
  timeout_s: 900

evidence_anchors:
  primary_score: rmsep
  primary_score_lower_is_better: true
  reference_models:
    - PLS-tuned-cv5
    - AOM-PLS-compact-numpy
    - POP-PLS-compact-numpy
