# Encoders

Planned modules:

- `cnn.py`: lightweight 1D CNN encoder;
- `patch_transformer.py`: wavelength-aware patch transformer;
- `sklearn_wrapper.py`: transform wrapper for downstream pipelines;
- `losses.py`: contrastive and auxiliary losses;
- `train.py`: bench training loop.

No pretrained weights should be committed here without an explicit artifact
policy.

