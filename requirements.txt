# Core scientific computing
# numpy>=2.0: ikpls uses ndarray.std(..., mean=...), added in NumPy 2.0.
numpy>=2.0.0
pandas>=2.2.2
scipy>=1.13.0
scikit-learn>=1.5.0

# Data processing
polars>=1.0.0
pyarrow>=14.0.0
h5py>=3.8.0

# Signal processing
PyWavelets>=1.4.0
pybaselines>=1.2.1

# Optimization & hyperparameter tuning
optuna>=3.0.0

# Utilities
joblib>=1.2.0
jsonschema>=4.17.0
pyyaml>=6.0.0
packaging>=23.0
pydantic>=2.0.0

# NIRS-specific splitting methods
kennard-stone>=2.2.0

# NIRS-specific modeling methods
ikpls>=1.1.0
pyopls>=20.0
trendfitter>=0.0.6

# dag-ml execution core — the DEFAULT engine since the ADR-17 cutover (a hard
# dependency, not an extra). run() falls back to legacy with a warning only when
# the native backend is unavailable.
dag-ml>=0.2.1
dag-ml-data>=0.2.1