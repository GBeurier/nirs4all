{
  "schema_version": 1,
  "inventory_complete": false,
  "unverified": [
    {"id": "warm_start_refit", "reason": "Legacy controllers implement warm-start; DAG-ML declares it unsupported. Reproduce a public training/refit run before classifying parity."},
    {"id": "tensorflow_custom_callbacks_cyclic_lr", "reason": "DAG-ML preflight rejects these controls; confirm the corresponding public legacy executions."},
    {"id": "native_methods_tuning", "reason": "Check legacy n4m tuning against DAG-ML host tuning with the same model and search space."},
    {"id": "optuna_pruner_parallel_phases_storage", "reason": "Legacy paths exist and DAG-ML rejects several controls; reproduce each public legacy contract and its native outcome."},
    {"id": "tf_jax_cli_export", "reason": "Current public framework tests do not force the CLI process and archive replay for each backend."},
    {"id": "by_source_generator_archive_semantics", "reason": "DAG-ML correctly refuses .n4a export of independent source-model outputs without a replayable fusion contract. Legacy writes an archive, but predicting on concatenated raw X gives first values 193.4067, 200.9121, 202.2472 on the two_source_distinct fixture; no source-fusion prediction oracle validates this replay. Define and test the intended merge/predict semantics before enabling export."}
  ],
  "documented_divergences": [
    {"id": "legacy_standard_scaler_operator_sweep_cv", "classification": "legacy globally fits preprocessing before CV", "pipeline": "[{'_or_': [StandardScaler(), MinMaxScaler()]}, KFold(2), Ridge(alpha=0.5)] on seeded 16x8 regression data", "legacy_cv_rmse": 0.183226613977017, "dagml_cv_rmse": 0.2787145504835322, "sklearn_fold_local_cv_rmse": 0.278715512091863, "sklearn_global_scale_cv_rmse": 0.18322674785759285, "evidence": "test_dagml_model_controls.py:test_operator_sweep_without_refit_keeps_all_cv_variants; direct sklearn fold-local vs full-pool StandardScaler oracles", "scope": "DAG-ML matches fold-local CV and has the same score with refit=True. Legacy globally fits StandardScaler on validation samples before each model's CV, yielding a different score."},
    {"id": "legacy_exclude_metadata_concat_score", "classification": "legacy selects a group-local CV row rather than scoring the merged OOF", "pipeline": "YOutlierFilter(iqr, threshold=1.0), KFold(3), by_metadata(group, PLSRegression(3)), merge=concat on seeded 120x20 balanced-group data with one train outlier", "legacy_cv_rmse": 0.8809393773365354, "dagml_cv_rmse": 0.9142813126624925, "sklearn_fold_local_cv_rmse": 0.9142813084180439, "evidence": "test_exclude_metadata_branch.py:test_exclude_then_metadata_branch_matches_per_group_oracle", "scope": "Legacy executes and reports its best group A CV row as cv_best_score. DAG-ML scores the reassembled OOF universe, matches the direct sklearn oracle, and excludes the outlier from training."}
  ],
  "gaps": [
    {"id": "operator_top_k_refit", "priority": "P2", "legacy": "top_k>1 refits multiple ranked operator-generator variants", "dagml": "top_k>1 currently supports parameter sweeps only and explicitly refuses operator variants", "evidence": "run_backend.py:_dispatch_run top_k guard; native operator-variant selection"},
    {"id": "cli_by_source_fusion", "priority": "P2", "legacy": "by_source fusion runs", "dagml": "forced CLI coordinator rejects CV+refit bundle", "evidence": "test_dagml_native_n4a_bundle.py:test_by_source_fusion_n4a_export_never_refits_on_legacy"}
  ]
}
